# Metrics

This directory defines how future sanitized aggregate engineering metrics may be represented. The JSON Schema provides a stable, reviewable contract without publishing metric values.

Metrics must be verified, approved for public release, and aggregated to avoid revealing sensitive operational details. No metrics dataset is included in the initial foundation.
