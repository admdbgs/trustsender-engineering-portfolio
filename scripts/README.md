# Scripts

This directory is reserved for future utilities that validate or generate public portfolio artifacts. Scripts should produce deterministic, reviewable output from sanitized inputs.

No scripts or dependencies are included in the initial foundation. Future automation must follow the repository safety rules and must not access private sources.
